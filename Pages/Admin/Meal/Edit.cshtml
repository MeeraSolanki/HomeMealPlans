@page
@model MealPlans.Pages.Admin.Meal.EditModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>TblMeal</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TblMeal.Id" />
            <div class="form-group">
                <label asp-for="TblMeal.MealName" class="control-label"></label>
                <input asp-for="TblMeal.MealName" class="form-control" />
                <span asp-validation-for="TblMeal.MealName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TblMeal.IsActive" class="control-label"></label>
                <input asp-for="TblMeal.IsActive" class="form-control" />
                <span asp-validation-for="TblMeal.IsActive" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TblMeal.IsDeleted" class="control-label"></label>
                <input asp-for="TblMeal.IsDeleted" class="form-control" />
                <span asp-validation-for="TblMeal.IsDeleted" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
